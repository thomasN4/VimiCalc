package vimicalc;

public class NumCell extends Cell {

    private int content;

    NumCell (int x_pos, int y_pos, int content) {
        this.x_pos = x_pos;
        this.y_pos = y_pos;
        this.content = content;
    }
}
