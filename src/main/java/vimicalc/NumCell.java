package vimicalc;

public class NumCell extends Cell {

    private int value;

    NumCell (int x_pos, int y_pos, int value) {
        this.x_pos = x_pos;
        this.y_pos = y_pos;
        this.value = value;
    }
}
